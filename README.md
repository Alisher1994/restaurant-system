# 🍴 Ресторанная Система Управления

Полноценная система управления рестораном с модульной архитектурой для каждой роли персонала.

## 🎯 Возможности

### Роли и Модули

- **Администратор** - управление пользователями, меню, столами, общая статистика
- **Кассир** - приём оплат, оформление чеков, закрытие заказов
- **Официант** - приём заказов от клиентов, привязка к столам
- **Повар** - просмотр очереди заказов, создание заявок на закупку
- **Снабженец** - приём заявок от поваров, оформление закупок
- **Склад** - оприходование товаров, списание, инвентаризация
- **Столы** - управление столами, бронирование (1, 2, 3 этажи + VIP)

## 📦 Технологии

- **Backend:** Node.js 16+, Express, PostgreSQL
- **Frontend:** Vanilla JavaScript, HTML5, CSS3
- **Auth:** JWT (JSON Web Tokens)
- **Database:** PostgreSQL 14+
- **Deploy:** Railway

## 🚀 Быстрый Старт (Development)

### 1. Клонирование и установка зависимостей

\`\`\`bash
git clone <repository-url>
cd restaurant-system
npm install
\`\`\`

### 2. Настройка PostgreSQL

Создайте базу данных и примените схему:

\`\`\`bash
psql -U postgres
CREATE DATABASE restaurant_db;
\\q

psql -U postgres -d restaurant_db < database-schema.sql
\`\`\`

### 3. Настройка переменных окружения

Создайте файл \`.env\`:

\`\`\`env
DATABASE_URL=postgresql://user:password@localhost:5432/restaurant_db
JWT_SECRET=your-super-secret-key-change-in-production
PORT=3000
NODE_ENV=development
\`\`\`

### 4. Запуск сервера

\`\`\`bash
npm start
\`\`\`

Откройте http://localhost:3000

### 5. Первый вход

Создайте администратора через API:

\`\`\`bash
curl -X POST http://localhost:3000/api/auth/register \\
  -H "Content-Type: application/json" \\
  -d '{
    "username": "admin",
    "password": "admin123",
    "full_name": "Администратор",
    "role": "admin"
  }'
\`\`\`

Или используйте SQL напрямую (пароль будет захеширован):

\`\`\`sql
-- Пароль: admin123
INSERT INTO users (username, password_hash, full_name, role) VALUES
('admin', '$2b$10$rXwJq5ZhFZxP.xY3yNxZYOXZGQJ8F4lC4MxXk3l2HnC0D6qE5G8Li', 'Администратор', 'admin');
\`\`\`

## 📋 Развертывание на Railway

### Шаг 1: Подготовка репозитория

\`\`\`bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/YOUR_USERNAME/restaurant-system.git
git push -u origin main
\`\`\`

### Шаг 2: Создание проекта в Railway

1. Зайдите на https://railway.app
2. Войдите через GitHub
3. Нажмите **"+ New Project"**
4. Выберите **"Deploy from GitHub repo"** → выберите ваш репозиторий

### Шаг 3: Добавление PostgreSQL

1. В Railway нажмите **"+ New"** → **"Database"** → **"Add PostgreSQL"**
2. PostgreSQL автоматически создастся

### Шаг 4: Подключение базы данных к приложению

1. Нажмите на ваш сервис (web)
2. **Variables** → **"+ New Variable"** → **"Add Reference"**
3. Выберите **Postgres** → **DATABASE_URL**
4. Добавьте также:
   - `JWT_SECRET` = (любая случайная строка)
   - `NODE_ENV` = `production`

### Шаг 5: Инициализация базы данных

После первого деплоя выполните SQL из `database-schema.sql`:

1. В Railway нажмите на **Postgres**
2. **Connect** → скопируйте **PSQL Command**
3. Выполните в терминале:

\`\`\`bash
psql <connection-string> < database-schema.sql
\`\`\`

Или через Railway CLI:

\`\`\`bash
railway connect postgres < database-schema.sql
\`\`\`

### Шаг 6: Генерация домена

1. Нажмите на web-сервис
2. **Settings** → **Networking** → **Generate Domain**
3. Скопируйте URL и откройте в браузере

## 📱 Использование

### Добавление пользователей

Администратор может добавлять пользователей через панель администратора или через API:

\`\`\`bash
curl -X POST https://your-app.up.railway.app/api/admin/users \\
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN" \\
  -H "Content-Type: application/json" \\
  -d '{
    "username": "waiter1",
    "password": "password123",
    "full_name": "Иван Официантов",
    "role": "waiter"
  }'
\`\`\`

### Роли пользователей

- `admin` - Администратор
- `cashier` - Кассир
- `waiter` - Официант
- `cook` - Повар
- `supplier` - Снабженец
- `warehouse` - Складской работник

## 🔧 API Endpoints (кратко)

### Авторизация
- `POST /api/auth/login` - Вход
- `POST /api/auth/register` - Регистрация (для тестов)

### Администратор
- `GET /api/admin/users` - Список пользователей
- `POST /api/admin/users` - Создать пользователя
- `GET /api/admin/menu-items` - Меню
- `POST /api/admin/menu-items` - Добавить блюдо
- `GET /api/admin/stats` - Статистика

### Официант
- `GET /api/waiter/tables` - Список столов
- `GET /api/waiter/menu` - Меню
- `POST /api/waiter/orders` - Создать заказ
- `GET /api/waiter/my-orders` - Мои заказы

### Кассир
- `GET /api/cashier/orders` - Заказы к оплате
- `POST /api/cashier/orders/:id/pay` - Принять оплату
- `GET /api/cashier/shift-stats` - Статистика смены

### Повар
- `GET /api/cook/orders` - Очередь заказов
- `POST /api/cook/orders/:id/start` - Взять в работу
- `POST /api/cook/orders/:id/complete` - Заказ готов
- `POST /api/cook/purchase-requests` - Создать заявку на закупку

### Снабженец
- `GET /api/supplier/purchase-requests` - Заявки на закупку
- `POST /api/supplier/purchases` - Оформить закупку

### Склад
- `GET /api/warehouse/inventory` - Складские остатки
- `POST /api/warehouse/inventory/:id/inbound` - Оприходовать
- `POST /api/warehouse/inventory/:id/outbound` - Списать

### Столы
- `GET /api/tables` - Все столы
- `POST /api/tables/reservations` - Забронировать

## 📊 Схема Базы Данных

Основные таблицы:
- `users` - Пользователи и роли
- `categories`, `menu_items` - Меню
- `tables`, `table_reservations` - Столы и бронирования
- `orders`, `order_items` - Заказы
- `inventory`, `inventory_movements` - Склад
- `purchase_requests`, `purchases` - Закупки
- `payments` - Платежи

Полная схема в файле `database-schema.sql`

## 🐛 Решение проблем

### Ошибка подключения к БД

Проверьте переменную `DATABASE_URL` в Railway:
\`\`\`bash
railway variables
\`\`\`

### Таблицы не созданы

Выполните SQL скрипт:
\`\`\`bash
railway connect postgres < database-schema.sql
\`\`\`

### Не могу войти

Создайте админа вручную через SQL (см. раздел "Первый вход")

## 📄 Лицензия

MIT

## 🤝 Поддержка

Если возникли вопросы - проверьте логи в Railway:
1. Нажмите на сервис
2. **Deployments** → выберите деплой → **View Logs**

---

**Готово! Система полностью функциональна и готова к использованию.** 🎉
